# Source code and development files
src/**/*.ts
src/test/**
*.map

# Build tool config
esbuild.js
tsconfig.json
eslint.config.mjs

# Dependencies and lock files
node_modules
.git
.gitignore
.env
pnpm-lock.yaml
package-lock.json

# Build tool config files (keeping vite configs as they might be needed)
vite.config.ts
vite.webview.config.ts
vitest.config.ts

# Include static assets - these must be in the package
!imgs/**
!imgs/logo.png
